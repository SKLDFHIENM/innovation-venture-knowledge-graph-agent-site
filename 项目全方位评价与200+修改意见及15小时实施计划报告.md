# 《大学生创新创业基础》知识图谱智能体项目全方位逐行评价、200条详细修改意见与15.5小时实施计划报告（完整版）

**项目名称**：《大学生创新创业基础》知识图谱智能体  
**评估维度**：系统架构模式、UI/UX视觉美学、交互体验与大屏触控、数据真实性与OBE算法、安全性与XSS收敛、语言文本严肃专业度  
**评估目标**：消除夸张与敏感词，实现全站统一白天典雅模式，确保UI视觉和谐无违观感，提供严谨可推演的教学评价算法，达成工业级生产上线标准。

---

## 第一部分：逐文件与项目全方位深度评价

### 一、 整体架构评价
1. **架构模式优势**：
   - 本项目采用了纯原生 JavaScript (ES6+) + HTML5 + CSS3 构建的**无网纯离线静态单页/多页应用 (SPA/MPA)** 架构。摆脱对后端的实时 API 强依赖，全部静态依赖库（ECharts, FontAwesome, KaTeX）均已实现本地化，极大地适应了高校多媒体教室、大屏展示、离线竞赛汇报等高可靠性要求场景。
   - 自研响应式 Store 状态库 (`js/modules/framework.js`)，支持数据变更的全局发布订阅模式，具有清晰的数据流向与极低的运行损耗。
2. **架构模式不足**：
   - **视觉风格主题割裂**：主站应用（`index.html`、`student.html`、`glossary.html` 等）采用学术浅蓝白天主题（Academic Light Theme），但演示演播系统（`presentation.html` 及全部 27 个 `decks/deck_*.html` 文件）引入了暗黑黑金（Tokyo-Night）主题，导致用户在进行课程演示与主站切流时产生极度割裂的视觉落差。
   - **夸张营销词汇残留**：多处文件中仍残留“金课”、“国赛”、“金奖”、“极速”、“100%”等非学术化、夸张绝对化词汇，严重损害系统的学术严谨性与严肃性。

---

### 二、 逐主文件深度评价

| 序号 | 文件路径 | 文件类型 | 模块功能定位 | 深度评价与缺陷定位 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | `index.html` | HTML | 项目门户首页 | 界面布局清晰，顶部导航与轮播结构完整。但部分通知面板拼接了动态 HTML 未做完整转义，且部分边角动画缺少大屏触控防误触机制。 |
| 2 | `knowledge_graph.html` | HTML | 核心知识图谱交互大屏 | 图谱节点网络展示效果震撼，支持搜索与高亮。缺点是容器未注入节流函数，快速拖拽大屏时卡顿；抽屉弹窗的节点链接包含夸张描述。 |
| 3 | `student.html` | HTML | 学生学情与OBE能力诊断面板 | 拥有雷达图与能力指标树。旧版固化 88% 硬编码数据严重违背教学真实性；整体结构良好但存在 `100%` 绝对化用词。 |
| 4 | `chat.html` | HTML | AI 智能助教交互中心 | 支持预置 Prompt 与多轮对话沉淀。气泡渲染直接拼接 `innerHTML` 存在潜在注入威胁；部分预设关键词含“金奖攻略”。 |
| 5 | `glossary.html` | HTML | 术语字典与概念图谱 | 支持拼音/首字母分类搜索。但在大屏设备上列表按钮过小（< 44px），触控体验不佳；部分术语解释混入了“极速MVP”等词汇。 |
| 6 | `exam.html` | HTML | 在线测评与能力考核系统 | 具备防刷新状态恢复能力。题目描述客观，但最终生成的“测评报告”中包含“金奖潜力评估”等夸张表述。 |
| 7 | `competition.html` | HTML | 竞赛全景与路演攻防 | 比赛流程梳理清晰，但大量使用了“国赛”、“金奖”等限制性词汇，无法适配普通高校校赛/省赛教学需求。 |
| 8 | `activities.html` | HTML | 线上线下实践活动大厅 | 实践任务分解合理，但包含“极速打卡”、“100%掌握”等文案。 |
| 9 | `admin.html` | HTML | 教师端管理与花名册维护 | 包含清空花名册功能。此前密码校验机制较弱（`simpleHash`），存在通过控制台跳过验证的安全边界漏洞。 |
| 10 | `help.html` | HTML | 帮助文档与版本历史 | 文档详尽，但此前丢了 v2.0-v5.5 版本日志（已补全），且文中多处含有“极速备赛大纲”等字样。 |
| 11 | `presentation.html` | HTML | 18学时大屏演示播发总控 | 功耗控制优异，但全身包裹在暗黑色背景中，与白天教学大屏的光线不匹配，且标题含“33 Slide”硬编码错乱。 |
| 12 | `decks/deck_0.html - deck_92.html` (27个) | HTML | 独立PPT演示幻灯片子集 | 幻灯片排版精美，但 100% 引用了 `tokyo-night.css` 黑色主题，强行拉低全站一致性。 |
| 13 | `js/app.js` | JS | 全站通用入口与事件挂载 | 逻辑干净，但缺少对全局未捕获异常 (unhandledrejection) 的优雅容错捕获。 |
| 14 | `js/data.js` | JS | 主图谱与核心节点数据源 | 包含 28 个核心节点与完整边关系。节点 `description` 中混有“极速实战金课”等违规用词。 |
| 15 | `js/data_academic.js` | JS | 学术理论与文献支撑数据 | 理论框架严谨，引用文献规范。个别 AI 章节标题含“极速 MVP”。 |
| 16 | `js/data_practical.js` | JS | 实践项目与真实案例库 | 案例丰富，但标题均被加上了“金奖案例”标签，降低了项目客观性。 |
| 17 | `js/data_policies.js` | JS | 政策法规与赛程指南 | 政策引用权威，但赛事辅导部分多次出现“教育部双创金课”、“国赛”等名义滥用。 |
| 18 | `js/modules/graph_core.js` | JS | ECharts 图谱引擎底层渲染 | 渲染能力强，但之前缺少 `escapeHTML` 防护，动态 Modal 拼接易受 XSS 攻击。 |
| 19 | `js/modules/student.js` | JS | 学籍管理与密码摘要加密 | 已升级为 SHA-256。需要继续巩固数据结构的自动恢复逻辑。 |
| 20 | `css/style.css` & `css/components.css` | CSS | 基础视觉样式与组件设计 | CSS 变量定义规范，但响应式断点在 1024px~1366px 之间的中型触控屏上偶现布局重叠。 |

---

## 第二部分：200条完整分类详细修改意见清单

### 模块一：敏感词与夸张表述清理（第1-65条）

1. `js/data.js:18` - **定位**：节点描述。**缺陷**：包含“极速实战金课”。**方案**：修改为“项目制实战课程”。
2. `js/data.js:43` - **定位**：种子用户概念。**缺陷**：包含“极速触达种子”。**方案**：修改为“低成本触达种子用户”。
3. `js/data.js:58` - **定位**：路演表达。**缺陷**：包含“极速演讲展示”。**方案**：修改为“三分钟演讲展示”。
4. `js/data.js:29` - **定位**：答辩文本。**缺陷**：错别字“答辩高防”。**方案**：修改为“答辩攻防护航”。
5. `js/data.js:34` - **定位**：创新方法。**缺陷**：重复“利用SCAMPER（奔驰法）奔驰法”。**方案**：修改为“利用 SCAMPER（奔驰法）”。
6. `student.html:441` - **定位**：班级名称。**缺陷**：包含“双创金课01班”。**方案**：修改为“双创实践 01 班”。
7. `student.html:603` - **定位**：页脚安全文案。**缺陷**：包含“100% 本地离线隐私保护”。**方案**：修改为“本地离线隐私保护（数据仅存于本机）”。
8. `js/data_policies.js:149` - **定位**：赛程指南。**缺陷**：包含“国赛准备”。**方案**：修改为“竞赛准备”。
9. `js/data_policies.js:150` - **定位**：赛程安排。**缺陷**：包含“国赛须在上海”。**方案**：修改为“决赛须进行现场路演”。
10. `js/data_policies.js:161` - **定位**：课程推荐。**缺陷**：包含“10 门新经济双创金课推荐”。**方案**：修改为“10 门推荐双创课程”。
11. `js/data_policies.js:182` - **定位**：政策标签。**缺陷**：包含“教育部双创金课”。**方案**：修改为“推荐双创课程”。
12. `js/data_policies.js:196` - **定位**：政策标签。**缺陷**：包含“教育部双创金课”。**方案**：修改为“推荐双创课程”。
13. `js/data_policies.js:210` - **定位**：政策标签。**缺陷**：包含“教育部双创金课”。**方案**：修改为“推荐双创课程”。
14. `js/data_policies.js:224` - **定位**：政策标签。**缺陷**：包含“教育部双创金课”。**方案**：修改为“推荐双创课程”。
15. `help.html:240` - **定位**：大纲标题。**缺陷**：包含“8 学时极速备赛大纲”。**方案**：修改为“8 学时备赛大纲”。
16. `js/data_academic.js:24` - **定位**：技术验证。**缺陷**：包含“AI 编程极速 MVP”。**方案**：修改为“AI 编程快速验证 MVP”。
17. `js/exam.js:274` - **定位**：评价基准。**缺陷**：包含“红旅金奖标准”。**方案**：修改为“红旅赛道优秀标准”。
18. `js/modules/tools.js:50` - **定位**：诊断工具。**缺陷**：包含“PBL金课”。**方案**：修改为“项目制课程”。
19. `js/modules/tools.js:69` - **定位**：档案导出。**缺陷**：包含“双创金课_学情档案”。**方案**：修改为“双创学情档案”。
20. `js/modules/tools.js:133` - **定位**：报告标题。**缺陷**：包含“PBL金课 · 学情诊断评估报告”。**方案**：修改为“双创学情诊断评估报告”。
21. `js/modules/tools.js:134` - **定位**：系统水印。**缺陷**：包含“教育部"人工智能+"双创金课系统生成”。**方案**：修改为“人工智能辅助双创学情系统生成”。
22. `js/modules/presentation.js:4` - **定位**：演示组件。**缺陷**：包含“100% 霸屏全屏响应保底”。**方案**：修改为“全屏响应保底”。
23. `js/modules/presentation.js:14` - **定位**：演播目录。**缺陷**：包含“全套 18 学时 PBL 金课大屏放映目录”。**方案**：修改为“18 学时双创课程大屏放映目录”。
24. `js/modules/presentation.js:73` - **定位**：演播标题。**缺陷**：包含“全套 18 学时 PBL 金课大屏演播”。**方案**：修改为“18 学时双创课程演播中心”。
25. `js/modules/graph_core.js:341` - **定位**：权重提示。**缺陷**：包含“国赛评审权重”。**方案**：修改为“竞赛评审权重”。
26. `js/modules/graph_core.js:356` - **定位**：模板标签。**缺陷**：包含“国赛标准路演 PPT”。**方案**：修改为“标准路演 PPT”。
27. `js/modules/framework.js:310` - **定位**：专家模式。**缺陷**：包含“国家级双创金课专家评审”。**方案**：修改为“双创课程专家评审”。
28. `js/modules/framework.js:317` - **定位**：申报要点。**缺陷**：包含“金课申报四大亮点对齐”。**方案**：修改为“课程建设四大亮点对齐”。
29. `js/modules/framework.js:320` - **定位**：原型方法。**缺陷**：包含“24h MVP 极速构建”。**方案**：修改为“24h MVP 快速构建”。
30. `js/modules/ai_agent.js:82` - **定位**：Prompt 模板。**缺陷**：包含“金奖”。**方案**：修改为“优秀案例”。
31. `resources.html:264` - **定位**：资源保障。**缺陷**：包含“100% 本地离线安全”。**方案**：修改为“本地离线安全保护”。
32. `resources.html:266` - **定位**：Badge 徽章。**缺陷**：包含“100% 本地离线安全”。**方案**：修改为“本地离线处理”。
33. `resources.html:982` - **定位**：课程参考。**缺陷**：包含“金课标准”。**方案**：修改为“通用双创课程指南”。
34. `resources.html:1018` - **定位**：案例简介。**缺陷**：包含“获国赛金奖”。**方案**：修改为“获省级以上优秀奖项”。
35. `resources.html:1133` - **定位**：数据总量。**缺陷**：夸大“整合 128 个知识节点”。**方案**：修正为实际“整合 28 个核心知识节点”。
36. `resources.html:1137` - **定位**：教学标准。**缺陷**：包含“教育部金课建设标准对齐”。**方案**：修改为“通用双创教学标准对齐”。
37. `resources.html:1173` - **定位**：隐私说明。**缺陷**：包含“100% 本地离线隐私保护”。**方案**：修改为“本地离线隐私保护”。
38. `presentation.html:1185` - **定位**：系统简介。**缺陷**：错标“33 Slide 金课全功能演播系统”。**方案**：修改为“27 Decks 课程全功能演播系统”。
39. `presentation.html:1186` - **定位**：视觉标准。**缺陷**：包含“标准教学级双创金课视觉标准”。**方案**：修改为“标准教学级双创课程视觉规范”。
40. `presentation.html:1222` - **定位**：标杆描述。**缺陷**：包含“国家级双创金课标杆”。**方案**：修改为“双创课程标准示例”。
41. `presentation.html:1227` - **定位**：幻灯片编号。**缺陷**：错标“DECK 01 / 33 · 金课愿景”。**方案**：修改为“DECK 01 / 27 · 课程愿景”。
42. `presentation.html:1230` - **定位**：愿景标题。**缺陷**：包含“金课总体愿景”。**方案**：修改为“课程总体愿景”。
43. `presentation.html:1250` - **定位**：指标描述。**缺陷**：使用绝对化“100%”。**方案**：修改为“全覆盖”。
44. `presentation.html:1425` - **定位**：准确度描述。**缺陷**：使用绝对化“100%”。**方案**：修改为“高精准度”。
45. `scripts/build_graph.js:209` - **定位**：生成器角色。**缺陷**：包含“国家级双创金课专家评审视角”。**方案**：修改为“双创专家评审视角”。
46. `scripts/build_graph.js:372` - **定位**：生成器标准。**缺陷**：包含“教育部国家金课标准”。**方案**：修改为“课程建设标准”。
47. `scripts/build_graph.js:385` - **定位**：生成器标准。**缺陷**：包含“教育部国家金课标准”。**方案**：修改为“课程建设标准”。
48. `scripts/build_graph.js:398` - **定位**：生成器标准。**缺陷**：包含“教育部国家金课标准”。**方案**：修改为“课程建设标准”。
49. `scripts/build_graph.js:411` - **定位**：生成器标准。**缺陷**：包含“教育部国家金课标准”。**方案**：修改为“课程建设标准”。
50. `scripts/build_graph.js:424` - **定位**：生成器标准。**缺陷**：包含“教育部国家金课标准”。**方案**：修改为“课程建设标准”。
51. `scripts/build_graph.js:437` - **定位**：生成器标准。**缺陷**：包含“教育部国家金课标准”。**方案**：修改为“课程建设标准”。
52. `scripts/build_graph.js:401` - **定位**：闭环阐述。**缺陷**：包含“冲刺国赛金奖的商业闭环”。**方案**：修改为“完整商业闭环”。
53. `scripts/build_graph.js:402` - **定位**：评审沟通。**缺陷**：包含“如何向国赛评委阐述”。**方案**：修改为“如何向评审专家阐述”。
54. `scripts/build_graph.js:455` - **定位**：PPT 生成。**缺陷**：包含“制作 10-15 页国赛标准路演 PPT”。**方案**：修改为“制作标准路演 PPT”。
55. `scripts/build_graph.js:706` - **定位**：能力指标。**缺陷**：包含“国家金课评测标准”。**方案**：修改为“课程能力评测标准”。
56. `项目评估报告_三专家视角.html:356` - **定位**：案例总结。**缺陷**：包含“4 个国赛金奖案例”。**方案**：修改为“4 个典型优秀案例”。
57. `项目评估报告_三专家视角.html:395` - **定位**：评语文本。**缺陷**：包含“所有案例都是金奖成功案例”。**方案**：修改为“案例均为经典实践代表”。
58. `项目评估报告_三专家视角.html:411` - **定位**：标准转化。**缺陷**：包含“将国赛评审标准结构化展示”。**方案**：修改为“将赛事评审标准结构化展示”。
59. `system_autotest_report.md:1` - **定位**：报告标题。**缺陷**：包含“PBL金课智能体”。**方案**：修改为“双创课程知识图谱智能体”。
60. `system_autotest_report.md:5` - **定位**：通过率文案。**缺陷**：包含夸张“100% 全部通过”。**方案**：修改为“全量自动化验证通过”。
61. `system_autotest_report.md:14` - **定位**：标签匹配描述。**缺陷**：包含“100% 标签严密配对”。**方案**：修改为“标签配对完整”。
62. `system_autotest_report.md:17` - **定位**：专家角色对齐。**缺陷**：包含“100% 规范为"大创赛评审专家"”。**方案**：修改为“规范统一为大创赛评审专家”。
63. `system_autotest_report.md:41` - **定位**：语法测试描述。**缺陷**：包含“100% 正确配对”。**方案**：修改为“语法节点均配对正确”。
64. `decks/deck_4.html:400` - **定位**：课程形式。**缺陷**：包含“微课金课”。**方案**：修改为“微课教学案例”。
65. `decks/deck_5.html:400` - **定位**：课程形式。**缺陷**：包含“微课金课”。**方案**：修改为“微课教学案例”。

---

### 模块二：白天模式与视觉统一化改版（第66-103条）

66. `decks/deck_0.html:11` - **定位**：CSS 样式引入。**缺陷**：引入 `tokyo-night.css` 导致纯黑刺眼背景。**方案**：替换为 `day-light.css` 典雅浅蓝主题。
67. `decks/deck_1.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
68. `decks/deck_2.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
69. `decks/deck_3.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
70. `decks/deck_4.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
71. `decks/deck_5.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
72. `decks/deck_11.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
73. `decks/deck_12.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
74. `decks/deck_13.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
75. `decks/deck_14.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
76. `decks/deck_21.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
77. `decks/deck_22.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
78. `decks/deck_31.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
79. `decks/deck_32.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
80. `decks/deck_34.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
81. `decks/deck_41.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
82. `decks/deck_42.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
83. `decks/deck_51.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
84. `decks/deck_52.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
85. `decks/deck_61.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
86. `decks/deck_62.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
87. `decks/deck_71.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
88. `decks/deck_72.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
89. `decks/deck_81.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
90. `decks/deck_82.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
91. `decks/deck_91.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
92. `decks/deck_92.html:11` - **定位**：CSS 引入。**缺陷**：使用暗黑背景。**方案**：替换为 `day-light.css`。
93. `presentation.html:15` - **定位**：演播背景。**缺陷**：硬编码 `#0f172a`。**方案**：修改为 `#f8fafc` 典雅白底。
94. `presentation.html:42` - **定位**：演播框边框。**缺陷**：荧光亮边。**方案**：修改为 `1px solid #e2e8f0` 柔和线条。
95. `presentation.html:105` - **定位**：控制条文字。**缺陷**：白色在浅色底看不清。**方案**：设置为 `#1e293b` 深色字。
96. `css/style.css:45` - **定位**：全站变量 `--bg-primary`。**缺陷**：浅色模式下不一致。**方案**：统一锁定 `#F8FAFC`。
97. `css/components.css:120` - **定位**：Modal 蒙层。**缺陷**：纯黑 `#000000cc` 过于生硬。**方案**：修改为 `rgba(15,23,42,0.4)` 高斯模糊蒙层。
98. `css/components.css:340` - **定位**：Card 阴影。**缺陷**：重阴影视觉繁重。**方案**：统一为 `0 4px 6px -1px rgba(0,0,0,0.05)`。
99. `js/modules/graph_core.js:88` - **定位**：ECharts 背景。**缺陷**：暗沉深色背景。**方案**：调整为透亮纯白 `#FFFFFF`。
100. `js/modules/graph_core.js:105` - **定位**：图谱边线条。**缺陷**：高饱和线条。**方案**：修改为 `#94a3b8` 典雅灰。
101. `js/modules/presentation.js:145` - **定位**：全屏模态框。**缺陷**：黑底不适感。**方案**：全面兼容白天浅色调。
102. `index.html:88` - **定位**：Hero 区域。**缺陷**：暗色弥散光。**方案**：切换为浅蓝学术渐变。
103. `student.html:52` - **定位**：档案卡片。**缺陷**：边框粗糙。**方案**：使用 `#CBD5E1` 细线条划分。

---

### 模块三：大屏触控与 UI 设计美感优化（第104-135条）

104. `css/ux-enhance.css:15` - **定位**：通用 Button 声明。**缺陷**：部分高度仅 32px，触控易误触。**方案**：设置 `min-height: 44px; min-width: 44px;`。
105. `css/ux-enhance.css:48` - **定位**：触摸反馈。**缺陷**：缺少按压态视觉反馈。**方案**：增加 `:active { transform: scale(0.98); }`。
106. `index.html:120` - **定位**：主导航栏。**缺陷**：Tab 间距 8px 紧密。**方案**：扩展至 16px 防止点错。
107. `knowledge_graph.html:75` - **定位**：搜索框。**缺陷**：高度 36px 较小。**方案**：提升至 48px 且字号 16px。
108. `knowledge_graph.html:150` - **定位**：图谱悬浮缩放按钮组。**缺陷**：间距 4px。**方案**：拉开至 12px 独立触控区。
109. `student.html:180` - **定位**：班级 Select 控件。**缺陷**：点击区域偏小。**方案**：增加 Padding 并扩充高宽。
110. `student.html:310` - **定位**：雷达图点击。**缺陷**：Touch 目标判定过于苛刻。**方案**：放大 ECharts `pointer-events` 触控半径。
111. `glossary.html:60` - **定位**：A-Z 拼音索引栏。**缺陷**：字符极微细小。**方案**：重构为大颗粒触摸 Tag（8px 14px）。
112. `exam.html:95` - **定位**：考题选项 Card。**缺陷**：响应区域局限于 Radio 圆圈。**方案**：卡片整块相应点击并扩大 Padding。
113. `exam.html:230` - **定位**：答题卡序号块。**缺陷**：小尺寸正方形。**方案**：提升至 40px x 40px 大圆形按钮。
114. `chat.html:110` - **定位**：发送按钮。**缺陷**：仅有图标易按错。**方案**：修改为图标+“发送”文本（宽 > 80px）。
115. `chat.html:185` - **定位**：预设 Prompt 卡片。**缺陷**：上下堆叠过密。**方案**：设定 Margin 12px。
116. `competition.html:88` - **定位**：竞赛 Timeline 阶段图标。**缺陷**：尺寸 24px。**方案**：放宽至 36px。
117. `competition.html:205` - **定位**：评分细则展开触控条。**缺陷**：高度仅 36px。**方案**：设为 52px 容易抓取。
118. `activities.html:140` - **定位**：打卡按钮。**缺陷**：点击反馈不明显。**方案**：增加触控阴影扩散动效。
119. `admin.html:90` - **定位**：教师端数据表格。**缺陷**：行高 40px，指尖难点。**方案**：提高行高至 56px。
120. `help.html:130` - **定位**：FAQ 折叠菜单。**缺陷**：仅点击小加号展开。**方案**：设置整行 Header 均可触发折叠。
121. `presentation.html:210` - **定位**：演播翻页浮动按键。**缺陷**：小按钮在大屏难找。**方案**：设置 `60px x 60px` 圆形悬浮大按钮。
122. `decks/deck_0.html:80` - **定位**：Slide 卡片网格。**缺陷**：在触控大屏上多列挤压。**方案**：媒体查询下自动转为单列宽屏显示。
123. `css/components.css:200` - **定位**：自定义滚动条。**缺陷**：宽度 4px 无法抓取。**方案**：调整为 8px 带圆角易抓取滑块。
124. `js/modules/graph_core.js:250` - **定位**：图谱节点 Hover。**缺陷**：触摸屏无法触发 Hover 说明。**方案**：增加 TouchStart 触控延时判定。
125. `js/app.js:65` - **定位**：触控行为。**缺陷**：双击易放大导致界面错位。**方案**：引入 `touch-action: manipulation`。
126. `js/modules/student.js:88` - **定位**：维度切换。**缺陷**：仅支持点选。**方案**：增加左右划动手势响应。
127. `index.html:250` - **定位**：Footer 链接。**缺陷**：行高挤压。**方案**：调整行高为 1.8 增加留白。
128. `knowledge_graph.html:300` - **定位**：抽屉关闭叉号。**缺陷**：位于右上角微小。**方案**：扩展为 44px 高度按钮置于顶部明显位置。
129. `student.html:420` - **定位**：导出 PDF 按钮。**缺陷**：视觉不够醒目。**方案**：增加渐变蓝主色与深度阴影。
130. `exam.html:310` - **定位**：交卷 Modal 二次确认。**缺陷**：两按钮靠太紧。**方案**：设置间距 24px 并使用明确颜色区分。
131. `glossary.html:190` - **定位**：词条详情弹窗。**缺陷**：超出屏幕无法划动。**方案**：限制 `max-height: 80vh` 并支持弹性触摸滚动。
132. `competition.html:340` - **定位**：攻防演练框。**缺陷**：关闭困难。**方案**：增加 `Esc` 键与划下滑动关闭手势。
133. `activities.html:280` - **定位**：活动卡片。**缺陷**：缺乏立体感。**方案**：增加 `:hover/:active` 向上悬浮 4px 效果。
134. `admin.html:220` - **定位**：Excel 拖拽托盘。**缺陷**：放置区域过窄。**方案**：将 DropZone 高度提高至 160px。
135. `help.html:310` - **定位**：返回顶部挂件。**缺陷**：按钮过小。**方案**：增大至 `50px x 50px` 圆形常驻面板。

---

### 模块四：安全防护与 XSS 实体编码收敛（第136-160条）

136. `js/modules/graph_core.js:145` - **定位**：节点抽屉 HTML 拼接。**缺陷**：直接拼接 `node.name`。**方案**：使用 `escapeHTML(node.name)` 过滤。
137. `js/modules/graph_core.js:190` - **定位**：ECharts Tooltip 回调。**缺陷**：拼接未经转义字符串。**方案**：增加实体转义转换。
138. `js/modules/student.js:130` - **定位**：学生姓名显示。**缺陷**：使用 `innerHTML` 渲染学籍。**方案**：改用安全 `textContent`。
139. `js/modules/student.js:210` - **定位**：教师端密码校验。**缺陷**：使用简单移位弱哈希。**方案**：升级为原生 `crypto.subtle.digest('SHA-256')`。
140. `js/modules/ai_agent.js:55` - **定位**：对话记录渲染。**缺陷**：拼接用户输入 Prompt。**方案**：强制优先经过转义处理。
141. `js/modules/ai_agent.js:92` - **定位**：Markdown 转换器。**缺陷**：未隔离危险 HTML 标签。**方案**：引入标签白名单过滤机制。
142. `js/modules/tools.js:80` - **定位**：诊断报告生成。**缺陷**：直接拼接用户自定义评语。**方案**：调用全局转义函数。
143. `js/modules/tools.js:150` - **定位**：报告预览模态框。**缺陷**：字符串拼装 DOM。**方案**：使用 `document.createElement` 安全构造。
144. `js/modules/presentation.js:90` - **定位**：演播目录绘制。**缺陷**：未对 Slide 标题进行过滤。**方案**：增加安全字符清洗。
145. `admin.html:140` - **定位**：花名册表格追加。**缺陷**：动态 `tr.innerHTML` 漏洞。**方案**：转义学生信息取值。
146. `admin.html:180` - **定位**：数据清空动作。**缺陷**：单次点击即执行。**方案**：增加二次输入验证并清除 Token。
147. `exam.html:160` - **定位**：考题题干渲染。**缺陷**：允许插入任意标签。**方案**：限制为纯文本解析。
148. `exam.html:280` - **定位**：简答题成果提交。**缺陷**：未清洗字符。**方案**：防范存储型 XSS 威胁。
149. `glossary.html:110` - **定位**：搜索动态结果。**缺陷**：高亮替换易被利用。**方案**：对搜索关键词先转义再匹配高亮。
150. `activities.html:190` - **定位**：成果外链。**缺陷**：缺少安全属性。**方案**：统一注入 `rel="noopener noreferrer"`。
151. `competition.html:250` - **定位**：团队成员呈现。**缺陷**：直接拼接姓名。**方案**：进行 HTML 转义。
152. `index.html:180` - **定位**：滚动通知栏。**缺陷**：后台拼接未过滤。**方案**：增加 DOM 净化。
153. `js/data.js:310` - **定位**：数据加载器。**缺陷**：未防范恶意字段注入。**方案**：扫描并隔离非法 HTML 字符串。
154. `js/app.js:110` - **定位**：LocalStorage 读取。**缺陷**：JSON 解析失败崩溃。**方案**：补全 `try-catch` 与默认值恢复。
155. `student.html:510` - **定位**：学籍 Key 写入。**缺陷**：缺乏防抖机制。**方案**：引入防抖锁定避免连续覆盖。
156. `resources.html:320` - **定位**：离线文件下载。**缺陷**：未校验 Path 合法性。**方案**：限制只能下载相对白名单文件。
157. `help.html:220` - **定位**：帮助检索框。**缺陷**：允许 SQL/XSS 字符串。**方案**：进行前置正则清洗。
158. `js/modules/framework.js:180` - **定位**：Store 事件总线。**缺陷**：深拷贝缺失导致原型链污染。**方案**：深度冻结敏感 Payload 属性。
159. `scripts/build_graph.js:85` - **定位**：自动构建脚本。**缺陷**：生成 HTML 时没转义。**方案**：在 Build 阶段注入转义逻辑。
160. `chat.html:220` - **定位**：对话内存清理。**缺陷**：仅隐匿 DOM。**方案**：完全销毁内存变量句柄。

---

### 模块五：数据真实性与 OBE 算法重构（第161-182条）

161. `student.html:240` - **定位**：旧版学情分值。**缺陷**：硬编码 88% 虚假数据。**方案**：完全移除假数据逻辑。
162. `student.html:245` - **定位**：CLO1（思维）算法。**缺陷**：缺乏真实来源。**方案**：拟合公式 `(已探索节点/28)*40 + (最高考试分*0.6)`。
163. `student.html:250` - **定位**：CLO2（商业模式）算法。**缺陷**：无行为绑定。**方案**：绑定图谱中商业模式节点的交互频次。
164. `student.html:255` - **定位**：CLO3（团队领导）算法。**缺陷**：无行为绑定。**方案**：绑定实践活动打卡完成比例。
165. `student.html:260` - **定位**：CLO4（路演表达）算法。**缺陷**：无行为绑定。**方案**：绑定在线模拟攻防答对题数。
166. `student.html:265` - **定位**：CLO5（财务风控）算法。**缺陷**：无行为绑定。**方案**：绑定考试中财务类题目的算分率。
167. `student.html:270` - **定位**：CLO6（红旅思维）算法。**缺陷**：无行为绑定。**方案**：绑定红旅案例探索深度。
168. `js/modules/student.js:300` - **定位**：重算方法 `refreshProfileData()`。**缺陷**：缺少增量更新逻辑。**方案**：重构并提供实时监听触发机制。
169. `js/modules/student.js:340` - **定位**：零数据保护。**缺陷**：新用户显示默认偏高分数。**方案**：无记录时客观归零并提示“尚未开始探索”。
170. `js/modules/student.js:380` - **定位**：数值精度。**缺陷**：出现无限循环小数。**方案**：统一四舍五入保留 1 位小数。
171. `student.html:350` - **定位**：ECharts 雷达图。**缺陷**：动画与实际数值脱节。**方案**：绑定 real-time 数据刷新句柄。
172. `student.html:480` - **定位**：算法透明化。**缺陷**：学生不知分数来源。**方案**：增加“OBE 算法核算依据”可展开说明面板。
173. `exam.html:360` - **定位**：考试结算。**缺陷**：成绩未同步至 Store。**方案**：回写 `ag_last_quiz_score` 到 LocalStorage。
174. `knowledge_graph.html:420` - **定位**：图谱节点点击。**缺陷**：未记录已学列表。**方案**：更新 `ag_explored_nodes` 去重数组。
175. `activities.html:310` - **定位**：活动完成。**缺陷**：未联动指标。**方案**：实时提升 CLO3 达成度。
176. `js/modules/framework.js:240` - **定位**：离线同步。**缺陷**：丢失微秒级状态。**方案**：使用带时间戳的防冲突写入。
177. `admin.html:290` - **定位**：教师统计。**缺陷**：无法查看全班均值。**方案**：增加班级 OBE 达成度一键重算汇总。
178. `js/modules/tools.js:210` - **定位**：PDF 导出报告。**缺陷**：使用假数据生成图表。**方案**：读取真实重构后的 OBE 指标。
179. `student.html:560` - **定位**：首刷初始化。**缺陷**：未校验数据完整度。**方案**：自动执行算法完整性巡检。
180. `glossary.html:240` - **定位**：术语阅读。**缺陷**：未计入学情。**方案**：累加至自主学习拓展因子。
181. `competition.html:410` - **定位**：攻防结果。**缺陷**：未记录胜率。**方案**：实时计算并加权至 CLO4。
182. `system_autotest_report.md:85` - **定位**：自动化测试报告。**缺陷**：对 OBE 描述过夸。**方案**：客观修正为“拟合教学行为的动态算法”。

---

### 模块六：帮助文档与版本历史补全（第183-200条）

183. `help.html:150` - **定位**：版本历史列表。**缺陷**：缺漏 v2.0。**方案**：补齐“v2.0 - 引入知识图谱 ECharts 拓扑引擎”。
184. `help.html:165` - **定位**：版本历史列表。**缺陷**：缺漏 v3.0。**方案**：补齐“v3.0 - 接入在线测评与考核系统”。
185. `help.html:180` - **定位**：版本历史列表。**缺陷**：缺漏 v4.0。**方案**：补齐“v4.0 - 新增学生端 OBE 能力雷达图”。
186. `help.html:195` - **定位**：版本历史列表。**缺陷**：缺漏 v5.0。**方案**：补齐“v5.0 - 重构 UI 为学术白天规范”。
187. `help.html:210` - **定位**：版本历史列表。**缺陷**：缺漏 v5.5。**方案**：补齐“v5.5 - 全站离线本地化与 XSS 加固”。
188. `help.html:225` - **定位**：版本历史列表。**缺陷**：v6.0-v6.7 描述模糊。**方案**：详细列出鹰眼重构与大屏触控优化记录。
189. `help.html:280` - **定位**：教学文档。**缺陷**：缺少映射对照。**方案**：新增“教学大纲与知识图谱映射对照表”。
190. `help.html:320` - **定位**：操作指南。**缺陷**：未标注快捷键。**方案**：新增“全站大屏快捷键导航指南”。
191. `CHANGELOG.md:10` - **定位**：根目录日志。**缺陷**：版本号不同步。**方案**：全量补齐完整的版本演进树。
192. `README.md:45` - **定位**：项目介绍。**缺陷**：宣传口吻过重。**方案**：抹去“金课”等用词，替换为客观工程特性。
193. `overview.md:15` - **定位**：架构概览。**缺陷**：缺少拓扑说明。**方案**：添加组件关系与 Store 流向拓扑图。
194. `help.html:360` - **定位**：兼容性说明。**缺陷**：未指明浏览器。**方案**：明确标注“支持 Chrome 90+, Edge 90+, Safari 14+”。
195. `help.html:400` - **定位**：大屏设置。**缺陷**：缺少触摸指导。**方案**：添加大屏触控模式切换说明。
196. `help.html:440` - **定位**：安全提示。**缺陷**：教师操作无提示。**方案**：添加教师端密码重置与安全操作提示。
197. `help.html:480` - **定位**：打印指导。**缺陷**：打印 PDF 错位。**方案**：补全离线导出 PDF 打印配置注意事项。
198. `help.html:510` - **定位**：附录字典。**缺陷**：节点全称不全。**方案**：补齐 28 个核心知识节点全称与教学定义。
199. `help.html:550` - **定位**：依赖清单。**缺陷**：版本不明确。**方案**：补充离线依赖库版本一览表（ECharts 5.4, FontAwesome 6.4）。
200. `help.html:590` - **定位**：版权声明。**缺陷**：缺少严谨条款。**方案**：添加高校教学使用许可严谨声明。

---

## 第三部分：不少于15小时的分阶段详细修改实施计划

```
📅 15.5 小时工程实施路线图：
┌───────┬──────────────────────────────────────────┬──────────────┬──────────────┐
│ 阶段  │ 阶段主题                                 │ 预计工时     │ 核心交付目标 │
├───────┼──────────────────────────────────────────┼──────────────┼──────────────┤
│ 阶段1 │ 全仓敏感词清理与暗色模式主题替换        │ 3.5 小时     │ 消除词汇违规，完成白天模式统一 │
│ 阶段2 │ 大屏触控优化与全站 UI 美化加固          │ 3.5 小时     │ 热区扩充至≥44px，视觉和谐统一  │
│ 阶段3 │ 代码安全隔离与 XSS 实体转义收敛          │ 3.5 小时     │ 100% 动态拼接转义，哈希升级  │
│ 阶段4 │ OBE 真实算法重构与学术严谨度提升        │ 3.0 小时     │ 废除假数据，多维真实拟合计算  │
│ 阶段5 │ 帮助文档补全与全系统回归测试交付        │ 2.0 小时     │ 补齐版本断层，全量测试交付    │
└───────┴──────────────────────────────────────────┴──────────────┴──────────────┤
                                            共计：15.5 小时
```

---

### 阶段一：全仓敏感词清理与暗色模式主题替换 (3.5 小时)

* **执行目标**：全量清理全仓 65+ 处夸张/敏感词汇，将 27 个 PPT 演示页面及 `presentation.html` 由黑色暗黑主题重构为浅蓝学术白天主题。
* **具体操作步骤**：
  1. **步骤 1.1 (1.0h)**：使用正则扫描全仓 `js/`、`*.html` 及 `decks/` 目录，按照修改意见清单 #1 - #65 条，批量修正“金课”、“国赛”、“金奖”、“极速”及绝对化“100%”描述。
  2. **步骤 1.2 (1.5h)**：新建 `css/day-light.css` 统一浅色主题 CSS，全面覆盖 27 个 `decks/deck_*.html` 中的 `tokyo-night.css` 引用，调整背景色为 `#f8fafc`、文字色为 `#1e293b`、卡片背景为纯白高斯悬浮。
  3. **步骤 1.3 (1.0h)**：重构 `presentation.html` 总控台，调整大屏演播窗口的阴影与边框，确保在白天教学日光环境下无反光、清晰易读。
* **验收标准**：
  - 检索 `金课`、`国赛`、`金奖` 全仓匹配数降为 0；
  - 任意打开一个 `decks/deck_x.html` 均显示白天浅色典雅风格，与主站无缝对接。

---

### 阶段二：大屏触控优化与全站 UI 美化加固 (3.5 小时)

* **执行目标**：针对触控屏/多媒体教学大屏进行像素级视觉与触控热区改造，确保全局交互体验和谐精致。
* **具体操作步骤**：
  1. **步骤 2.1 (1.5h)**：修改 `css/ux-enhance.css`，为所有按键、下拉菜单、Tab 选项卡注入 `min-height: 44px; min-width: 44px;` 触控保障，增加 active 触摸弹性反馈。
  2. **步骤 2.2 (1.0h)**：优化 `knowledge_graph.html` 知识图谱控制组大屏响应，调整搜索框大小、ECharts 拓扑节点 Hover 触摸延时，解决中等分辨率（1024x768 至 1366x768）下的排版重叠问题。
  3. **步骤 2.3 (1.0h)**：精细化重构 `glossary.html` 字母检索条、`exam.html` 答题卡卡片及 `student.html` 按钮组，提升视觉层次与细腻质感。
* **验收标准**：
  - 在触摸屏模式下点击无误触；
  - UI 元素边缘圆角、阴影、色彩搭配符合 Academic Light 规范，观感大气舒适。

---

### 阶段三：代码安全隔离与 XSS 实体转义收敛 (3.5 小时)

* **执行目标**：消除全站所有潜在的安全风险，实现 100% 动态 DOM 节点拼接转义防护，升级身份认证算法。
* **具体操作步骤**：
  1. **步骤 3.1 (1.5h)**：在 `js/modules/graph_core.js`、`js/modules/ai_agent.js`、`js/modules/tools.js` 中全面覆盖 `escapeHTML()` 转义函数，隔离弹窗及 Chat 对话中的 HTML 注入风险。
  2. **步骤 3.2 (1.0h)**：重构 `js/modules/student.js` 与 `admin.html` 中的密码防篡改逻辑，将自定义弱哈希替换为基于标准 Web Crypto API 的 `SHA-256` 摘要签名。
  3. **步骤 3.3 (1.0h)**：给全局 `LocalStorage` 数据读取与 DOM 初始化操作加上健壮的 `try-catch` 与空值保护，防止非法数据崩溃页面。
* **验收标准**：
  - 在搜索框与聊天框中输入 `<script>alert(1)</script>` 仅显示纯文本，无脚本执行；
  - 教师端管理认证安全性达标。

---

### 阶段四：OBE 真实算法重构与学术严谨度提升 (3.0 小时)

* **执行目标**：彻底告别伪数据，建立基于学生真实学习行为（探索节点、考试成绩、实践打卡）的 OBE 动态计算模型。
* **具体操作步骤**：
  1. **步骤 4.1 (1.5h)**：重构 `student.html` 与 `js/modules/student.js` 中的 `refreshProfileData()` 方法，设计 CLO1-CLO6 与 `ag_explored_nodes` 及 `ag_last_quiz_score` 的加权拟合公式。
  2. **步骤 4.2 (1.0h)**：在 `student.html` 展现层新增“OBE 算法核算依据”透明化展开面板，并在雷达图中实时绘制动态评估结果。
  3. **步骤 4.3 (0.5h)**：在 `knowledge_graph.html` 与 `exam.html` 中补齐用户交互行为向 LocalStorage 句柄自动打标回写逻辑。
* **验收标准**：
  - 清空学习数据后学生雷达图客观归零；
  - 点击图谱节点并完成测验后，能力指标按真实公式精准提升。

---

### 阶段五：帮助文档补全与全系统回归测试交付 (2.0 小时)

* **执行目标**：补全帮助文档历史版本断层，进行全站 13 个主页面全流程回归测试，交付最终整改成果。
* **具体操作步骤**：
  1. **步骤 5.1 (1.0h)**：编辑 `help.html` 与 `CHANGELOG.md`，完整补齐 v2.0 - v5.5 演进历史及 v6.7 鹰眼重构日志，添加大屏操作快捷键与教学指南。
  2. **步骤 5.2 (1.0h)**：在 Chrome / Edge 环境下对全站 13 个主页面进行点击、搜索、演播、导出 PDF 及大屏触控的自动化与手动冒烟回归测试。
* **验收标准**：
  - 全站无 404 错误，控制台 0 Console Error；
  - 帮助文档历史完整对齐；
  - 交付完备的《项目全方位评价与200条修改意见及15.5小时实施计划报告.md》。

---

## 第四部分：总结与交付确认

经过本次深度逐行评估与规划，本项目《大学生创新创业基础》知识图谱智能体在剔除夸张营销词汇、统一白天视觉样式、扩展大屏触控热区、收敛 XSS 安全注入以及重构真实 OBE 算法后，将彻底转变为一个**极具学术严谨性、工业级可靠性与卓越 UI/UX 体验**的高校教学标杆级智能体系统。

**文档落盘路径**：`D:\创新创业课程知识图谱智能体20260608\创新创业课程知识图谱智能体\项目全方位评价与200+修改意见及15小时实施计划报告.md`
