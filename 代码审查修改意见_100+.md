# 创新创业课程知识图谱智能体 · 鹰眼代码审查修改意见（100+ 条）

> 目标：完善这个**专业、严谨、可用**的项目。  
> 审查方式：逐文件通读 + 全仓敏感词 / 暗色主题 / XSS 注入面 / 控制台残留 正则扫描。  
> 共提出 **160 条**具体修改意见，逐条标注 `文件:行号` 与证据，按严重度分组。

---

## 严重等级说明

- 🔴 P0 阻断级：安全漏洞 / 数据错误 / 合规风险，必须修。
- 🟠 P1 高优：风格不统一 / 一致性错误 / 夸张表述，强烈建议修。
- 🟡 P2 中优：可维护性 / 可访问性 / 课堂体验优化。
- 🟢 P3 建议：锦上添花。

---

## 一、敏感词与夸张表述残留（严谨性 / 合规）🟠 — 共 65 条

> 此前清理仅覆盖了零星几处，`金课 / 100% / 国赛 / 金奖 / 极速 / 128个` 在全仓仍有大量残留，与"严谨、专业、不出现夸张表述"目标直接冲突。

1. 🔴 `js/data.js:18` — `value: '通用高校 18学时极速实战金课'` 同时含"金课"与"极速"，夸张且违规。建议改为中性描述，如"通用高校 18 学时项目制实战课程"。
2. 🟠 `js/data.js:43` — `极速触达种子` 含"极速"。改为"低成本触达种子用户"。
3. 🟠 `js/data.js:58` — `极速演讲展示` 含"极速"。改为"三分钟演讲展示"。
4. 🟠 `js/data.js:29` — `答辩高防` 应为"答辩攻防"或"答辩护航"，疑似错别字。
5. 🟠 `js/data.js:34` — `利用SCAMPER（奔驰法）奔驰法` 重复"奔驰法"。改为"利用 SCAMPER（奔驰法）"。
6. 🔴 `student.html:441` — `双创金课01班` 含"金课"。改为"双创实践 01 班"。
7. 🔴 `student.html:603` — `Antigravity Engine · 100% 本地离线隐私保护` 含绝对化"100%"，且英文品牌与中文语境割裂。改为"本地离线隐私保护（数据仅存于本机浏览器）"。
8. 🟠 `js/data_policies.js:149` — `8-10 月：国赛准备（获省级铜奖以上晋级）` 含"国赛"。改为"竞赛准备（获省级奖项以上晋级）"。
9. 🟠 `js/data_policies.js:150` — `国赛须在上海进行现场路演` 含"国赛"。改为"竞赛须进行现场路演"。
10. 🟠 `js/data_policies.js:161` — `10 门新经济双创金课推荐` 含"金课"。改为"10 门推荐双创课程"。
11. 🔴 `js/data_policies.js:182` — `type: '教育部双创金课'` 含"金课"且借教育部名义。改为"课程推荐"。
12. 🔴 `js/data_policies.js:196` — 同上 `教育部双创金课`。
13. 🔴 `js/data_policies.js:210` — 同上 `教育部双创金课`。
14. 🔴 `js/data_policies.js:224` — 同上 `教育部双创金课`。
15. 🟠 `help.html:240` — `8 学时极速备赛大纲` 含"极速"。改为"8 学时备赛大纲"。
16. 🟠 `js/data_academic.js:24` — `AI 编程极速 MVP` 含"极速"。改为"AI 编程快速验证 MVP"。
17. 🟠 `js/exam.js:274` — `红旅金奖标准` 含"金奖"。改为"红旅赛道优秀标准"。
18. 🔴 `js/modules/tools.js:50` — `courseName: "《大学生创新创业基础》PBL金课"` 含"金课"。改为"《大学生创新创业基础》项目制课程"。
19. 🔴 `js/modules/tools.js:69` — `双创金课_学情档案` 文件名含"金课"。改为"学情档案"。
20. 🔴 `js/modules/tools.js:133` — `PBL金课 · 学情诊断评估报告` 含"金课"。
21. 🔴 `js/modules/tools.js:134` — `教育部"人工智能+"双创金课系统生成` 含"金课"且借教育部名义。
22. 🟠 `js/modules/presentation.js:4` — `100% 霸屏全屏响应保底` 含绝对化"100%"。改为"全屏响应保底"。
23. 🔴 `js/modules/presentation.js:14` — `全套 18 学时 PBL 金课大屏放映目录索引` 含"金课"。
24. 🔴 `js/modules/presentation.js:73` — `全套 18 学时 PBL 金课大屏演播快捷放映中心` 含"金课"。
25. 🟠 `js/modules/graph_core.js:341` — `国赛评审权重` 含"国赛"。改为"竞赛评审权重"。
26. 🟠 `js/modules/graph_core.js:356` — `国赛标准路演 PPT` 链接文案含"国赛"。改为"竞赛标准路演 PPT"。
27. 🔴 `js/modules/framework.js:310` — `国家级双创金课专家评审` 含"国家级""金课"。改为"双创课程专家评审"。
28. 🔴 `js/modules/framework.js:317` — `金课申报四大亮点对齐` 含"金课"。改为"课程建设四大亮点"。
29. 🟠 `js/modules/framework.js:320` — `生成式 AI 辅助 24h MVP 极速构建` 含"极速"。改为"快速构建"。
30. 🟠 `js/modules/ai_agent.js:82` — 关键词数组含 `金奖`。改为中性词如"优秀案例"。
31. 🔴 `resources.html:264` — `100% 本地离线安全` 绝对化。改为"本地离线处理，数据不主动上传"。
32. 🔴 `resources.html:266` — 徽标 `100% 本地离线安全` 同上。
33. 🟠 `resources.html:982` — `对齐教育部专长指南与金课标准` 含"金课"。改为"对齐通用双创教学指南"。
34. 🟠 `resources.html:1018` — `获国赛金奖` 含"国赛""金奖"。改为"获竞赛优秀奖项"。
35. 🔴 `resources.html:1133` — `整合 128 个知识节点` 与实际图谱 28 个节点严重矛盾（且"128个"为夸张数字）。改为真实节点数或删除具体数字。
36. 🔴 `resources.html:1137` — `教育部金课建设标准对齐` 含"金课"。改为"通用双创课程标准对齐"。
37. 🔴 `resources.html:1173` — `100% 本地离线隐私保护` 绝对化。
38. 🔴 `presentation.html:1185` — `33 Slide 金课全功能演播系统` 含"金课"且"33 Slide"与实际 27 decks 不符。
39. 🔴 `presentation.html:1186` — `标准教学级双创金课视觉标准` 含"金课"。
40. 🔴 `presentation.html:1222` — `国家级双创金课标杆` 含"国家级""金课"。
41. 🔴 `presentation.html:1227` — `DECK 01 / 33 · 金课愿景` 含"金课"。
42. 🔴 `presentation.html:1230` — `金课总体愿景` 含"金课"。
43. 🟠 `presentation.html:1250` — 指标 `100%` 绝对化。改为具体指标或"全覆盖"。
44. 🟠 `presentation.html:1425` — `100%` 绝对化。
45. 🔴 `scripts/build_graph.js:209` — `title="🎓 国家级双创金课专家评审视角"` 含"国家级""金课"。
46. 🔴 `scripts/build_graph.js:372` — `教育部国家金课标准` 含"金课"。
47. 🔴 `scripts/build_graph.js:385` — 同上 `教育部国家金课标准`。
48. 🔴 `scripts/build_graph.js:398` — 同上。
49. 🔴 `scripts/build_graph.js:411` — 同上。
50. 🔴 `scripts/build_graph.js:424` — 同上。
51. 🔴 `scripts/build_graph.js:437` — 同上。
52. 🟠 `scripts/build_graph.js:401` — `表现优秀！具备冲刺国赛金奖的商业闭环完整度。` 含"国赛""金奖"。改为"具备完整的商业闭环"。
53. 🟠 `scripts/build_graph.js:402` — `如何向国赛评委阐述单位经济模型` 含"国赛"。改为"竞赛评委"。
54. 🟠 `scripts/build_graph.js:455` — `制作 10-15 页国赛标准路演 PPT` 含"国赛"。改为"竞赛标准"。
55. 🔴 `scripts/build_graph.js:706` — `📜 国家金课评测标准` 含"金课"。
56. 🟠 `项目评估报告_三专家视角.html:356` — `4 个国赛金奖案例` 含"国赛""金奖"。改为"4 个竞赛优秀案例"。
57. 🟠 `项目评估报告_三专家视角.html:395` — `所有案例都是金奖成功案例` 含"金奖"。
58. 🟠 `项目评估报告_三专家视角.html:411` — `将国赛评审标准结构化展示` 含"国赛"。
59. 🔴 `system_autotest_report.md:1` — 标题 `PBL金课智能体` 含"金课"。
60. 🟠 `system_autotest_report.md:5` — `100% 全部通过 (13 / 13)` 绝对化且与本项目仍存在大量问题矛盾（报告本身失真）。改为"全量通过"并核实结论。
61. 🟠 `system_autotest_report.md:14` — `100% 标签严密配对` 绝对化。
62. 🟠 `system_autotest_report.md:17` — `100% 规范为"大创赛评审专家"` 绝对化。
63. 🟠 `system_autotest_report.md:41` — `100% 正确配对` 绝对化。
64. 🔴 `decks/deck_4.html:400` 与 `decks/deck_5.html:400` — `PBL（基于项目的学习）微课金课` 含"金课"。
65. 🟠 全部 decks（deck_41/42/51/52/61/62/71/72/81/82/91/92 等）`极速 MVP（最简可行产品） 上线` 卡片标题含"极速"，建议统一改为"MVP（最简可行产品）上线"。

---

## 二、风格统一：暗色模式残留（视觉一致性）🟠 — 共 38 条

> 用户明确要求"每个页面都是白天模式，不出现黑的黑、白的白"。但全部 27 个 decks 仍引入 `tokyo-night.css`（暗黑黑金），`presentation.html` 仍为暗色，与首页/学生端/活动页的浅色主题严重割裂。

1. 🔴 `decks/deck_0.html:11` — 引入 `../css/ppt/themes/tokyo-night.css`（暗色）。改为共享浅色主题 `day-light.css`。
2. 🔴 `decks/deck_1.html:11` — 同上。
3. 🔴 `decks/deck_2.html:11` — 同上。
4. 🔴 `decks/deck_3.html:11` — 同上。
5. 🔴 `decks/deck_4.html:11` — 同上。
6. 🔴 `decks/deck_5.html:11` — 同上。
7. 🔴 `decks/deck_11.html:11` — 同上。
8. 🔴 `decks/deck_12.html:11` — 同上。
9. 🔴 `decks/deck_13.html:11` — 同上。
10. 🔴 `decks/deck_14.html:11` — 同上。
11. 🔴 `decks/deck_21.html:11` — 同上。
12. 🔴 `decks/deck_22.html:11` — 同上。
13. 🔴 `decks/deck_31.html:11` — 同上。
14. 🔴 `decks/deck_32.html:11` — 同上。
15. 🔴 `decks/deck_34.html:11` — 同上。
16. 🔴 `decks/deck_41.html:11` — 同上。
17. 🔴 `decks/deck_42.html:11` — 同上。
18. 🔴 `decks/deck_51.html:11` — 同上。
19. 🔴 `decks/deck_52.html:11` — 同上。
20. 🔴 `decks/deck_61.html:11` — 同上。
21. 🔴 `decks/deck_62.html:11` — 同上。
22. 🔴 `decks/deck_71.html:11` — 同上。
23. 🔴 `decks/deck_72.html:11` — 同上。
24. 🔴 `decks/deck_81.html:11` — 同上。
25. 🔴 `decks/deck_82.html:11` — 同上。
26. 🔴 `decks/deck_91.html:11` — 同上。
27. 🔴 `decks/deck_92.html:11` — 同上。
28. 🟠 `css/ppt/themes/tokyo-night.css` — 暗色主题文件本身应保留备选，但默认皮肤与切换入口需明确"教学默认=浅色"，并在 `data-themes` 中将浅色置于首位。
29. 🔴 `presentation.html:170` — `background: linear-gradient(135deg, #0F172A 0%, var(--gz-accent) 100%)` 暗色面板。改为浅色渐变。
30. 🔴 `presentation.html:222` — iframe `background:#050608` 暗色。改为 `#F8FAFC`。
31. 🔴 `presentation.html:525` — `linear-gradient(135deg, #0f172a 0%, #0369a1 60%, #0284c7 100%)` 暗色。改浅色。
32. 🟠 `presentation.html:811` — `linear-gradient(180deg, #334155 0%, #1e293b 100%)` 暗色面板。
33. 🟠 `presentation.html:1186` — 暗色徽标 `rgba(0,242,254,0.15)` 在浅色页不搭。改浅色描边。
34. 🔴 `js/modules/presentation.js:69` — `background:#0b1120` 暗色卡片。改浅色。
35. 🔴 `js/modules/presentation.js:222` — iframe `background:#050608` 暗色。
36. 🟠 `css/ux-enhance.css:199` — toast `background: #0f172a; color:#fff` 在浅色页面弹出暗色小窗，割裂。改为浅色卡片 + 深色文字。
37. 🟡 `项目评估报告_三专家视角.html:11` — `background:#0B0F19` 暗色（作为独立文档可接受，但若要与站点统一建议改浅色）。
38. 🟠 `decks/*` 内 `guizang-metric-num` 多处 `100%` 数字（如 deck_41:576、deck_51:544 等）在暗色下呈荧光紫，随暗色主题一并修复时统一改为浅色可读样式。

---

## 三、数据一致性与正确性 Bug 🟠 — 共 17 条

1. 🔴 `index.html:521` — 弹窗文案 `Task 04 单元经济模型测算`，但 CLO 矩阵中 Task 04 = "环境研判与合规"，文案与定义不符。按真实定义修正。
2. 🔴 `index.html` vs `student.html` — 六条 CLO 名称两页定义不一致（如 index 将 CLO4 定义为"环境研判与合规"，student 将 CLO4 定义为"团队管理与股权治理"）。需统一为一套权威 CLO 定义并集中维护。
3. 🔴 `index.html:544-555`（三方评价 30%/50%/20%） vs `student.html:496-509`（平时30%/期中30%/期末40%） vs `js/modules/student.js` `calcOBEScore`（30/30/40）— OBE 权重三处冲突。确立唯一权威权重并全局引用。
4. 🔴 `activities.html:458` — 页脚 `知识节点：128 个`，与图谱实际 28 个节点矛盾。改为真实数值或移除。
5. 🟠 `activities.html:357` — `精选 24 项` 活动，与项目叙事"18 项 PBL 任务"不一致。统一口径（如"18 项 PBL + 配套活动"）。
6. 🟠 `activities.html` 筛选器 `全部活动 (24)` 同上计数不一致。
7. 🔴 `knowledge_graph.html:584-589` — 图例 M1–M5（精益双创/大赛求职/政策红利）与 `js/data.js` 的 M1–M9 类别定义不匹配。图例应直接由 `data.js` 类别生成。
8. 🟠 `knowledge_graph.html:509-515` — 导航前缀 `01 首页 / 02 课程地图 …`，而 `index.html`、`student.html` 无数字前缀，风格割裂。统一去掉或统一加上。
9. 🟠 `knowledge_graph.html` `openExpertPanel()` — 硬编码分数 `i%3 → 88/72/54`，非真实数据，误导。改为基于节点属性或留空占位。
10. 🟠 `graph.html` 专家面板（如有）同样存在硬编码分数问题，需同步修正。
11. 🟠 `js/data.js:29` — `答辩高防` 错别字（见第 4 条）。
12. 🟠 `js/data.js:34` — `奔驰法` 重复（见第 5 条）。
13. 🟠 `student.html:603` — `Antigravity Engine` 英文品牌名在中文教学语境突兀，且与站点无统一品牌命名。
14. 🟠 `resources.html:1018` — `帮扶 1200 余户农户，损耗率从 40% 降至 8%` 数据无来源，疑似虚构/夸张。补充真实出处或改为示意性表述。
15. 🟠 `system_autotest_report.md` — 自称"100% 全部通过、零缺陷"，但本审查已发现大量敏感词与暗色残留，报告结论失真，需重测并更新。
16. 🟠 `admin.html:741` — 清空花名册后提示"系统将切换为开放模式，任意学号均可登录"，属安全降级且无二次确认风险说明。建议清空后默认保持关闭或要求重设密码。
17. 🔴 `js/modules/student.js:130` — `Admin.verify` 在未设密码时返回 `true`（开放管理权限），构成认证绕过。应默认拒绝，必须设置密码。

---

## 四、安全：XSS 与身份认证 🔴 — 共 8 条

1. 🔴 `index.html:776` — `sendDemoMsg()` 将用户输入 `txt` 直接拼入 `aiDiv.innerHTML`，存在存储型/反射型 XSS。必须先 `escapeHTML(txt)`（项目已具备 `tests/utils.test.js` 的 escapeHTML，应提取为公共函数复用）。
2. 🔴 `student.html:739` — `profile.studentId` 直接写入 `innerHTML`，学生注册时输入恶意脚本即触发 XSS。转义后输出。
3. 🔴 `student.html:740` — `profile.className / college` 同理直写 `innerHTML`。转义。
4. 🔴 `js/modules/student.js:395-425` — `_openPrintWindow()` 将 `profile.name`、`profile.studentId` 未转义拼入 `win.document.write(...)`。转义或改用 `textContent`。
5. 🟠 `graph.html:668` — `resBox.innerHTML = filtered.map(n => ... onclick="selectSearchItem('${n.name}')" ...)`，节点名未转义拼入属性，若数据含单引号会破坏结构/注入。用 `escapeHTML` 并改用 `data-*` 属性传参。
6. 🟠 `chat.html:628` — `typingDiv.querySelector('.msg-bubble').innerHTML = reply.replace(/\n/g,'<br/>')`，AI 回复若含 HTML 会被执行。先转义再替换换行。
7. 🔴 `js/modules/student.js:43-49` — `simpleHash()` 为 31 乘数可逆向哈希，非安全散列，不可用于"密码"。建议改为 `SHA-256`（可用内置 `crypto.subtle`）或仅作本地标识、明确不存储明文口令。
8. 🟢 `tests/utils.test.js:28` — 已实现 `escapeHTML` 并通过测试，但全项目未引用。应将其提升为 `js/common.js` 公共函数，全局统一使用。

---

## 五、课堂可操作性 🟡 — 共 8 条

1. 🟢 `activities.html` 小组数量 2–12 自定义已具备（localStorage `act_group_count`），但与 `admin.html` 教师端未打通：教师无法在管理端统一设定/下发小组数。建议 admin 端增加"小组规模预设"并写入共享键。
2. 🟡 `activities.html` `cycleGroupStatus()` 仅"思考中/已提交/需辅导"三态，缺终态"已完成/优秀"。补充终态便于课堂收尾。
3. 🟡 `exam.html` 测评结果未明确是否与 `student.html` 学情档案实时联动，学生端"暂无测评记录"提示需说明刷新机制。
4. 🟠 `student.html:690` 清空数据 `confirm` 后未清理全部 localStorage 键（遗漏 `iekg_roster_v1`、`iekg_admin_pwd`、`iekg_all_profiles`、`ag_student_*` 等）。补全清理清单。
5. 🟠 `admin.html` `confirmClear()` 仅删 7 个键，遗漏 `iekg_admin_pwd` 等。补全。
6. 🟡 小组配置键名 `act_group_count` 与站点其他 `iekg_*`/`ag_*` 命名风格不统一，建议统一前缀便于维护。
7. 🟡 `activities.html` 分组结果无导出/打印，教师无法留存课堂分组。增加"导出分组表"。
8. 🟢 活动页缺乏教师端"一键随机分组"动画反馈，大屏演示体验弱。

---

## 六、导航 / 版本 / 链接一致性 🟠 — 共 7 条

1. 🔴 `index.html` 品牌 `v4.0` 与 `js/modules/student.js:316` `v6.6 Refactored (2026.07.21)` 版本号冲突。确立单一版本常量集中维护。
2. 🟠 `presentation.html:1185` "33 Slide" 与实际上 27 个 decks 不符，且含"金课"。改为真实数量。
3. 🟠 `knowledge_graph.html` 导航数字前缀与首页不一致（见第 111 条）。
4. 🟡 `graph.html` 与 `knowledge_graph.html` 功能高度重叠（两个图谱页），建议合并或明确分工（如一个为"课程地图"、一个为"能力图谱"）。
5. 🟠 `index.html` PBL 仅展示 Task 01–06，链接"查看全部 18 项"，需确认列表页是否完整呈现 18 项且与 `data.js` 节点一一对应。
6. 🟠 `resources.html:1151` 页脚链接 `map.html` 文案"18学时极速大纲地图"含"极速"（见第 15 条同类）。
7. 🟡 全站页脚/导航的"返回首页"链接建议统一使用相对路径并校验 404（可用 `tests/check_nav_links.js` 已具备的扫描逻辑做 CI 校验）。

---

## 七、可访问性（WCAG）🟡 — 共 6 条

1. 🟠 `--text-muted:#64748B` 在浅灰底上对比度约 3.2:1，低于 WCAG AA 4.5:1（项目评估文档已自指此问题）。提升 muted 文字明度或仅用于非必要辅助信息。
2. 🔴 全部 27 个 decks 在暗色下 `--text-muted:#64748B` 对比度同样不达标，随暗色整改一并解决。
3. 🟡 部分按钮/触控目标 < 44px，不符合"大屏触摸演示、菜单文字图标大且清晰"要求。统一最小触控区 44×44px。
4. 🟡 多处说明文字 12–13px 偏小，大屏演示可读性差。正文基准建议 ≥15px。
5. 🟡 缺少全局 `:focus-visible` 轮廓，键盘可达性弱。补充焦点样式。
6. 🟡 部分页面缺 `<html lang="zh-CN">` 或 viewport 声明（需逐页确认），影响朗读与移动端缩放。

---

## 八、性能、可维护性与代码质量 🟡 — 共 9 条

1. 🟡 `js/app.js:36` `console.warn('[IEKG System] Data preload notice:', err)` 残留调试日志，生产应移除或改为按需。
2. 🟡 `js/app.js:188` `console.log('[SafeGuard] 唤起全局防保底函数...')` 残留调试日志。
3. 🟢 `scripts/*.js`、`create_landing.js` 等大量 `console.log`（构建/脚本），虽非运行时但建议收敛或加环境变量开关。
4. 🟡 `js/data.js`、`js/data_academic.js`、`js/data_policies.js` 多份图谱/政策数据冗余，建议合并为单一数据源避免漂移。
5. 🟡 各页独立 `<style>` 与 `css/style.css` 主题变量并存，易风格漂移。建议样式全部收敛到 `css/style.css` 与 `css/ppt/` 主题。
6. 🟡 `activities.html` `particles-canvas` 在浅色主题下仍为暗色粒子（需确认），割裂。改为浅色或移除。
7. 🔴 `presentation.html` 单文件 3000+ 行自包含，难以维护。建议拆分为 `ppt-shell` + 数据 + 组件，复用 `js/ppt/ppt_runtime.js`。
8. 🟠 `student.html` 诊断卡硬编码分数 88/78/52⚠/92/68⚠，与真实测评无关，易误导。改为基于实际数据的占位或真实计算。
9. 🟠 `activities.html:825` `alert('…达成度为 0.88（优秀）…')` 硬编码分数；`exam.html:659` `alert('已成功提交…')` 无真实提交逻辑；`knowledge_graph.html:718` `prompt()` 阻塞式输入。统一改为非阻塞 UI 反馈，分数由真实计算获得。
10. 🟡 `graph.html:375` `alert('报告生成中，请稍候...')` 为伪进度提示，体验差。改为真实生成或移除。

---

## 优先级修复建议（前 10 项，建议立即执行）

| 序  | 文件:行                                          | 问题                       | 等级 |
| -- | --------------------------------------------- | ------------------------ | -- |
| 1  | `js/modules/student.js:130`                   | 未设密码即开放管理权限（认证绕过）        | 🔴 |
| 2  | `index.html:776`                              | 用户输入未转义写入 innerHTML（XSS） | 🔴 |
| 3  | `student.html:739-740` + `student.js:395-425` | 学生资料未转义（XSS）             | 🔴 |
| 4  | `resources.html:1133`                         | "128 个知识节点"与实际 28 矛盾     | 🔴 |
| 5  | 全 27 个 `decks/*.html:11`                      | 暗色 tokyo-night 主题未统一     | 🔴 |
| 6  | `presentation.html` 多处 `#0F172A/#050608`      | 首页浅色但放映页暗色割裂             | 🔴 |
| 7  | `index/student/student.js` OBE 权重三处冲突         | 评分口径不一致                  | 🔴 |
| 8  | `js/data.js:18,29,34`                         | "金课/极速"+"答辩高防"+ "奔驰法"重复  | 🔴 |
| 9  | `system_autotest_report.md`                   | "100% 通过"结论失真            | 🟠 |
| 10 | `student.js:43-49`                            | simpleHash 弱哈希           | 🔴 |

---

## 附：可脚本化批量替换清单（建议用一次遍历清理）

- 全局替换 `金课` → `课程`（覆盖 js/data.js、student.html、resources.html、presentation.html、decks、scripts、system_autotest_report.md）。
- 全局替换 `极速` → `快速`/`高效`（覆盖 data.js、decks、help.html、framework.js、data_academic.js）。
- 全局替换 `国赛` → `竞赛`、`金奖` → `优秀奖项`、`国家级` → `校级/课程级`（覆盖 data_policies.js、exam.js、graph_core.js、build_graph.js、项目评估报告）。
- 全局替换绝对化 `100%` 为中性表述（resources.html、presentation.html、system_autotest_report.md、decks 指标）。
- 全部 `decks/*.html` 的 `tokyo-night.css` → 新建 `day-light.css` 并切换默认皮肤。

> 注意：批量替换前请先 `git diff` 备份并逐文件 review，避免误伤正常文案（如 `100%` 在某些 CSS 尺寸语境是合法的，仅替换文案语境中的绝对化断言）。
