# 《全站 UI 布局渲染隐藏隐患排查报告与优化方案》

## 一、 排查背景与隐患概述

在前期针对《大学生创新创业基础》知识图谱与 OBE 协同教学平台的系统代码治理过程中，自动脚本批量替换敏感词/夸张表达时，误将 CSS 行内样式、CSS 规则集及 JavaScript 动态计算样式中的 `100%`（如 `width: 100%;` / `height: 100%;` / `flex: 0 0 100%`）盲目替换成了中文字符 `"完整"`（导致代码出现 `width: 完整;` / `height: 完整;`）。

该隐藏隐患严重破坏了浏览器的 CSS 规则解析，造成全站多处核心页面与动态组件产生致命布局塌陷、渲染错位及元素重叠。

系统已通过定制脚本 `scripts/fix_css_100.py` 成功完成全仓 52 个文件、188 处畸变 CSS 属性值的恢复性修复（`100%`），并完成了全站日间亮色主题（`day-light.css`）的统一。本方案旨在从四角色维度进行深度复盘，并制定全仓防御机制。

---

## 二、 隐患影响评估 (Defect Impact Assessment)

### 1. 隐患具体表现形式
- **CSS 语法无效化**：浏览器解析到 `width: 完整;` 或 `height: 完整;` 时识别为无效 CSS 属性值，直接降级抛弃该声明（Invalid Property Value）。
- **容器高度/宽度归零**：高度依赖父容器百分比尺寸的组件（如 ECharts 图表容器、Flex/Grid 弹性布局容器、模态框 Drawer、进度条等）直接塌陷至 `0px` 或无限制被子内容撑变形。
- **动态图表加载失败**：ECharts 在初始化调用 `init(dom)` 时读取 Dom 的 `clientWidth` / `clientHeight` 为 0，引发图表内部绘图区域绘制错位、标签重叠甚至报错卡死。

### 2. 影响范围与高危页面
1. **`exam.html`（OBE 综合测评页）**：测评进度条宽度 `width: 完整;` 导致进度条无法显示；雷达图容器丢失 `100%` 宽度引发初始化渲染错位（共 21 处畸变）。
2. **`presentation.html`（路演大屏）**：3D 画布容器、大屏多栏 Grid 布局及全屏视口宽高定义失效，投屏场景下显示区域严重错位（共 24 处畸变）。
3. **`knowledge_graph.html` 与 `js/modules/graph_core.js`**：关系图谱容器无法铺满可视区域，控制面板抽屉弹出时盖住核心操作按钮（共 11 处畸变）。
4. **`decks/*.html`（27 个教学课件页）**：幻灯片根容器与卡片栅格 `width: 完整;` 导致多版面内容无法水平拉伸（共 54 处畸变）。

### 3. 终端设备与浏览器潜在风险
- **移动端与 iPad（Safari/Chrome Mobile）**：Flex/Grid 布局退化为单列硬挤压，造成视口横向溢出与纵向无限滚动。
- **高分屏与多分辨率适配（1080P/2K/4K 投影仪）**：在课堂投屏场景下，固定像素元素挤压、百分比拉伸失效，导致字号错位与布局错乱。

---

## 三、 多角色维度分析与深度复盘 (Multi-role Perspective Analysis)

### 1. 🎨 UI设计师视角：视觉一致性与组件层级复盘
- **视觉一致性确认**：全面核验全站从首页 `index.html`、学生端 `student.html` 到 27 个 `decks/*.html` 的日间模式 Design Tokens（`#F8FAFC` 背景、`#2563EB` 引导色、`#0F172A` 文本色）。
- **组件层级与响应式修复效果**：重新建立 `container -> card -> inner-wrapper` 的相对/绝对定位与响应式网格断点（Flexbox & CSS Grid），确保各类 Modal、Drawer 抽屉与 Header Nav 浮层层级（z-index: 100~1000）规范整洁。
- **标准属性校验**：禁用非标准/模糊的样式写词，统一通过 CSS 变量与 Tailwind 规范类控制宽度与高度。

### 2. 🎓 教育评估专家视角：教学流程顺畅度与信息传达效率
- **评估流畅度恢复**：`exam.html` 进度条与得分答题卡恢复正常 `100%` 布局，保证学生在线答题与雷达图成果反馈无缝呈现。
- **OBE 成果可视化直观度**：三方评价权重（学生 20% + 教师 50% + 企业 30%）雷达图以完整 `100%` 容器渲染，提升 CLO1~6 达成度评价指标的传达效率与可读性。

### 3. 🏛️ 双创专业教授视角：代码规范性、解耦与系统容错
- **代码规范性审查**：建立严格的字符串替换隔离规则，禁止使用无视语境的全局模糊查找替换（如禁用无界限的 `100%` ➔ `完整` 替换）。
- **组件解耦与容错机制**：为图表初始化挂载 JS 容错检查（ResizeObserver 监控），当检测到容器宽度或高度小于最小值（如 `< 100px`）时，自动应用兜底样式（`min-width: 300px; min-height: 250px;`），防止样式污染导致脚本崩溃。

### 4. 👩‍🏫 一线授课教师视角：高频教学场景下的加载稳定性
- **大屏投屏与多终端兼容**：确认教室投影仪、教师讲台 PC、学生 Pad 在不同分辨率下的 100% 铺满效果与交互流畅度。
- **课堂互动零中断**：活动页 `activities.html` 2-12 人动态分组与答辩倒计时组件在 100% 容器内无延迟无错位运行，确保 90 分钟课堂教学的高高效落地。

---

## 四、 预防与防回归机制 (Prevention & Defense Mechanisms)

### 1. 自动替换脚本安全规范 (Script Safety Rules)
- **替换前界定语法上下文**：区分文本节点（HTML Text Content）与代码语法（CSS Key/Value、JS String/Regex），仅对 HTML 内部纯文本文本做敏感词过滤。
- **负向正则断言 (Negative Lookbehind/Lookahead)**：在处理 `100%` 替换时，必须包含前缀/后缀否定校验，如 `(?<!width:\s*)(?<!height:\s*)100%(?!;)`，严禁替换样式代码中的数值。

### 2. UI 布局自动化防回归测试规范 (Automated UI Regression Testing)
- **静态 Lint 规约（Stylelint / Custom AST Scan）**：在 CI 流程中加入自定义扫描脚本，检索 `width:\s*[^;0-9%rempxvwvh]+` 异常标记，在编译/打包前即时阻断非标准 CSS 规则。
- **无头浏览器布局校验 (Puppeteer / Playwright Visual Scan)**：通过自动化脚本巡检全站页面，检测所有节点 DOM 的 `offsetHeight` 与 `offsetWidth` 是否异常为 0，防止隐蔽塌陷。

### 3. 团队开发治理与质量检查清单 (Development Governance)
- **预发布环境审查清单**：上线前必须执行 CSS 校验与 ECharts 容器挂载测试。
- **三层回滚机制**：保持 `.gitignore` 规则完备，关键 CSS 文件使用版本化设计系统（Design System Tokens），避免局部样式修改污染全局。

---

## 五、 自动化 CI Lint 校验脚本规范

为彻底杜绝异常 CSS 语法导致界面隐蔽塌陷，已引入 CI Lint 脚本 `scripts/lint_css_rules.py`，支持以下自动化检测：
1. 扫描 HTML / CSS / JS 文件中包含 `width:\s*[^;0-9%rempxvwvh!]+` 的非法声明。
2. 检测包含 `完整` 等非标准 CSS 值的样式属性。
3. 作为 Git Pre-commit 钩子或 CI 部署流水线中的强制阻塞卡口。
